export interface Anime{
    id:number,
    titulo:string,
    resumo:string,
    quantidadeEpisodios:number,
}

export interface NovoAnime {
    titulo:string,
    resumo:string,
    quantidadeEpisodios:number,
}